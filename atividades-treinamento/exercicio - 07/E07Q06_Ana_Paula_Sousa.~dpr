program E07Q06_Ana_Paula_Sousa;

{$APPTYPE CONSOLE}

{Calcule a área de círculos através da fórmula A=PI*R2, onde R representa o raio
e PI o número 3,14159.
Repetir o processo enquanto R for positivo.
Automatize o cálculo utilizando uma função específica para calcular
a área do círculo.}

uses
  SysUtils;

const
  fPI = 3.14159;

function CalcularAreaCirculo(fRaio: Double): Double;
begin
  Result := fPI * (fRaio * fRaio);
end;

var
  fRaio, fArea: Double;

begin
  repeat
    Write('Digite o raio do circulo (ou um valor negativo para sair): ');
    Readln(fRaio);
    
    if fRaio > 0 then
    begin
      fArea := CalcularAreaCirculo(fRaio);
      Writeln('A area do circulo com raio ', fRaio:0:2, ' e ', fArea:0:2);
    end;
  until fRaio <= 0;

  Writeln('Programa encerrado.');
end.
